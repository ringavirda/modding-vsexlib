namespace ExpandedLib.Definitions;

/// <summary>Catalogue of exlib's own block codes that multiblock layouts are drawn from.
/// <see cref="VanillaCodes"/> holds the game's own codes.</summary>
public static class ExCodes {
  /// <summary>The invisible per-cell filler a mega-block places over its own footprint.</summary>
  public const string Filler = ExlibBlocks.Structurefiller.Code;
}
