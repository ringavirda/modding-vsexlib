using ExpandedLib.Registries;

namespace SmokeStack;

/// <summary>Registers this assembly's config and every attribute-marked class or code-first
/// definition, via <see cref="ExModSystem"/>.</summary>
public class SmokeStackModSystem : ExModSystem { }
